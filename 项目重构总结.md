# 项目重构总结

## 📁 新的项目结构

```
strategy/
├── README.md                          # 项目总览
├── requirements.txt                   # 依赖
├── .gitignore
│
├── common/                            # 共享模块 ✨
│   ├── __init__.py
│   ├── data_fetcher.py               # 数据获取（共享）
│   └── metrics.py                    # 通用指标计算（IRR、夏普比率等）
│
├── config/                            # 全局配置 ✨
│   ├── __init__.py
│   └── global_config.py              # 全局配置（数据源、token等）
│
├── strategies/                        # 策略目录
│   ├── __init__.py
│   │
│   ├── macd/                         # MACD策略 ✨
│   │   ├── __init__.py
│   │   ├── config.py                 # MACD专属配置
│   │   ├── strategy.py               # 策略实现
│   │   ├── main.py                   # 运行入口
│   │   └── results/                  # MACD结果
│   │
│   └── position_management/          # 仓位管理策略 ✨
│       ├── __init__.py
│       ├── config.py                 # 仓位管理专属配置
│       ├── strategy.py               # 策略实现
│       ├── main.py                   # 运行入口
│       ├── run_all_periods.sh        # 批量运行脚本
│       ├── docs/                     # 仓位管理文档
│       │   ├── 仓位管理.md
│       │   ├── 仓位管理使用指南.md
│       │   ├── 多市场周期回测对比.md
│       │   └── 实现总结.md
│       └── results/                  # 仓位管理结果（包含历史回测）
│
├── scripts/                          # 工具脚本 ✨
│   └── (待添加)
│
└── tests/                            # 测试 ✨
    └── __init__.py
```

## 🔄 主要改进

### 1. 模块化设计
- ✅ 每个策略独立成包，包含自己的配置、代码、文档和结果
- ✅ 共享代码提取到 `common/` 目录
- ✅ 全局配置独立到 `config/` 目录

### 2. 代码优化
- ✅ 移除重复的IRR计算代码，统一使用 `common.metrics`
- ✅ 清理无用指标（保留了相对初始投入收益率用于对比，但主要展示净投入收益率和IRR）
- ✅ 更新所有导入路径

### 3. 结果管理
- ✅ 结果按策略分类存储
- ✅ 结果文件可以上传到git，保留历史记录

### 4. 文档组织
- ✅ 文档按策略组织，放在各自的 `docs/` 目录下
- ✅ 每个策略有独立的文档体系

## 🚀 如何运行

### MACD策略
```bash
cd strategies/macd
python main.py
```

### 仓位管理策略
```bash
cd strategies/position_management
python main.py

# 或运行所有周期
./run_all_periods.sh
```

## 📊 核心指标说明

### 保留的指标
1. **净投入收益率** ⭐ - 真实收益，基于净投入计算
2. **IRR年化收益率** ⭐⭐ - 最准确，考虑时间价值
3. **相对初始投入收益率** - 用于对比参考
4. **平均成本收益率** - 持仓盈亏
5. **资金使用效率** - 资金利用情况
6. **最大回撤** - 风险指标

### 移除的无用指标
- ❌ 基于累计投入的收益率（已被净投入收益率替代）
- ❌ 其他冗余的相对表现指标

## 🎯 下一步优化建议

1. 添加策略对比工具到 `scripts/`
2. 添加自动化测试到 `tests/`
3. 创建统一的报告生成工具
4. 添加更多通用指标到 `common/metrics.py`

## 📝 注意事项

- 运行策略时需要在策略目录下执行，或者使用绝对路径
- 所有策略共享 `common/` 中的数据获取和指标计算功能
- 结果文件现在会保存在各自策略的 `results/` 目录下
